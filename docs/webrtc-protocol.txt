title [DRAFT] CardDAO WebRTC Protocol


note over Player1: Status: Ready to play

Player1->Player2: Shuffled and encrypted deck + nonce + sig
Player1<-Player2: Re-shuffled and re-encrutped deck  + selected cards for player1 (positions) + nonce + sig
Player1->Player2: selected cards for player2 (secrets) + nonce + sig
Player2->Player1: ACK + nonce + sig

note over Player1,Player2: Game can now Start